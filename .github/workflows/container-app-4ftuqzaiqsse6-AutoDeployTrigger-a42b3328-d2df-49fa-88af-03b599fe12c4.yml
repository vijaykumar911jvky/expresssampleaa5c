name: Trigger auto deployment for container-app-4ftuqzaiqsse6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-4ftuqzaiqsse6-AutoDeployTrigger-a42b3328-d2df-49fa-88af-03b599fe12c4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP4FTUQZAIQSSE6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP4FTUQZAIQSSE6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP4FTUQZAIQSSE6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP4FTUQZAIQSSE6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP4FTUQZAIQSSE6_REGISTRY_PASSWORD }}
          containerAppName: container-app-4ftuqzaiqsse6
          resourceGroup: expresssampleaa5c888b
          imageToBuild: default/container-app-4ftuqzaiqsse6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


